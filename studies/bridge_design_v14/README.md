# Bridge-information design v14

This is a conditional provenance/design calculation, not a new drug-response predictor. It continues v13 without refitting old models or inventing biological pairings.

The supplied `bridge.py` exhaustively evaluates which one native P100 record should be traced to its authenticated RNA partner, ASSUMING three genuinely one-to-one mother-preparation records in both assays. Equal record counts do not establish that assumption. It enumerates all possible answers instead of selecting an expression-based match as truth. The actual number of authenticated links obtained in this run is zero.

In the full consolidated conversation source package, `run.py`, `verify.py`, exact v13 input marginals, all conditions and priority lists are included. That package restores 15 prior source archives with their original hashes. This remote directory does not silently claim that old evidence-only branches contain their missing archives.

On 389 eligible triplicate conditions out of the inherited433, the fixed20-analyte design gives a median75.596% worst-case range reduction within a condition IF the requested link is truly authenticated. Under equal cost and one link per condition,16 prioritized verifications remove21.425% of the full-cohort cell-macro COVARIANCE ambiguity, versus2.598% expected for uniform condition choice. This is NOT a prediction accuracy improvement, biological signal recovery, financial saving or confidence interval. Reweighting the endpoint to equal-condition CORRELATION reduces the analogous figures to3.502% versus2.783%, and the two top16 priority lists share only one condition. Retain both endpoints; do not choose an estimand because its design gain looks larger.

Forty-four non-triplicate conditions remain in the full-cohort range; none is changed to zero. Population sample-size/power and future paired experimental results are not estimated from this calculation. Full tests and independent LP comparisons are in the integrated archive. No Nature or modern-SOTA claim is made.
