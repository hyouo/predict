# v6 annotation and model lock

Registered before validation model scores. Official Reactome release-97 GMT acquired from https://download.reactome.org/97/ReactomePathways.gmt.zip (298479 bytes; zip SHA256 8c1dbc8578431da5d2d5118262718c60b553a9be3398e93658daa069e4a9afd4; text SHA256 89983d5c1f0af11c52edfeee7323eb425580ac6281d387a528562ab1787ce56b). GitHub Actions 34215938219 succeeded. This is unsigned pathway co-membership, not a drug-target or directed regulatory network.

On the unchanged 978 unique L1000 landmark symbols, keep human GMT sets overlapping 5 to 100 measured genes, collapse identical landmark memberships. Result: 658 unique sets, 738 covered genes, 8876 membership edges. This uses no outcomes. Negative control: bipartite double-edge randomization, seed 2026090801, 88760 accepted swaps, preserving every gene's membership degree and every pathway's landmark set size exactly; 675 original edges remain.

Kernel geometry: globally source-standardized baseline expression, same standard-deviation floor as v5. Full = dot product /978. Aggregate = pathway-projected features, source-only trace-normalized. Local = each output gene uses baseline genes sharing pathways; each pathway contributes inverse-size co-membership, remove self-edges, row-normalize, and use the global uniform weighting for uncovered genes. Self = own-gene baseline only. Mixture is convex with the full kernel. No output genes or response labels are selected by annotation.

Families locked: legacy_full (arithmetic-mean plug-in), universal_full, universal_aggregate, universal_local, plugin_local, universal_self, null_aggregate, null_local. The last two use the same degree-preserving randomized membership. Compare both unpenalized-intercept conditioning and the original plug-in rule. Penalties {0.01,0.1,1,10,100}; mixture {0,0.25,0.5,0.75,1} for every non-full family; output strengths {0.25,0.5,0.75,1} for every family. Zero and unscaled source mean remain separate fixed baselines. Select by validation macro MSE and, separately, by validation reference-matched replicated interaction excess risk; preserve both selectors in reports. Predictors see no held-context treatment labels. All selected final predictions will be frozen before v6 test scoring.

The official GMT is newly incorporated external information, but the numerical data and target labels are previously exposed development resources. This experiment tests the incremental utility of this specific prior and estimator, not scientific novelty of kriging, pathway kernels, or a general SOTA claim. Implementation smoke tests before this lock produced predictions but did not evaluate outcomes. Two preparation timeouts were caused by an inefficient diagnostic after data serialization; records and corrected replay are retained.
