# Transfer v8: joint precision and published-model adapter

Retrospective development on the already exposed 30-context LINCS L1000 Phase II slice. No new independent validation or superiority claim. Original source SHA256 34d198df9eddac5b535a0408794caaf41073fa014c74429d1050e483576fb581; fixed 18/6/6 contexts, 978 genes, 10 uM/24 h. No target treatment labels in fitting, representation, stopping, selection or refit. Main and source-tissue-excluded scenarios remain separate. All predictors receive identical allowed source effects and target baseline; missing profiles remain missing. Test scoring requires joint prediction hashes and common-support masks.

## Hypothesis 1
Does learning a cross-drug mean jointly with the same-drug residual covariance improve on v7's independently fitted mean? Compare: centered RBF, original source-response v7 mean, equal-observation OLS mean (control for weighting), joint GLS mean. Same 12-context-PC and 32-drug-SVD features, no output truncation. Kernel penalties [.01,.1,1,10,100], prior penalties [.1,1,10], blend [0,.5,1], scalar amplitude [0,1] selected analytically on validation contexts. GLS minimizes sum over drugs of (Y-XB)' Q (Y-XB) plus ridge, with Q=C^-1-C^-1 1(1' C^-1 1)^-1 1' C^-1 and C=K+kernel_penalty I. Predict with the exact original RBF plus mean innovation q_t-I(q_s), not q_t alone. OLS uses Q=I-11'/n and the same remaining rules. Joint GLS is a classical semi-parametric Gaussian/kernel model, not a new theorem.

## Published comparator feasibility study
Use the pinned author TranSiGen class at e7582e7794f13d4dfddfb15e95e3ba7cd24bc07a (MIT), exact architecture and six-term loss. Execute ECFP4-2048/random-initialization variant: encoder1200, decoder800, latent100, feature-embedding400, beta .1, dropout .1, Adam lr1e-3/weight-decay1e-5, batch64, seed364039. Hard budget64 epochs, evaluate each4; choose on validation-context effect MSE, refit allowed sources for that epoch count. This budget, task, aggregation, stopping and expression pairing differ from the original 300-epoch LINCS2020 paper. It must be labelled a task-adapted bounded author-core run, NOT a canonical full paper reproduction nor evidence that the published method is inferior. Train on observed source context/compound mean paired expressions (source control B with matching treatment), not synthetic cells. Predict from target A baseline. Eight fixed-seed Monte Carlo forwards approximate mean prediction; preserve stochastic uncertainty as computational, not biological. Unique exact-CID chemistry subset is declared; unannotated standalone effects default to zero, fused models default to RBF and must report covered-subset scores.

To match query-source information, include TranSiGen mean plus the same residual RBF innovation, selecting the same kernel/blend/amplitude grid on validation only. Direct raw and calibrated effect scores are both retained. No KPGT/shRNA-pretrained performance is claimed. Record learning curves, finite losses, source coverage, adaptation details and any failure/cap contact.

## Reporting
Retain total/centered/bias MSE, amplitude, drug retrieval, replicated reference-matched interactions, worst context and annotation coverage. Conditional context/drug resampling fixes fitted models and has no independent-biological interpretation; no new score can be treated as untouched confirmation. Preserve all code/data/source hashes, selection tables, predictions and failed runs. No default model replacement, automatic merge, paid GPU or unattended research.
