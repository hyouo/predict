# Post-scoring source-response identity diagnostic

Date 2026-09-09. Timing: after completion of the joint full/sparse-support scores. This is an explicitly post-scoring diagnostic on an already-used retrospective dataset, not new confirmation. No estimator selection, hyperparameter retuning, or changes to the primary scores.

The response-program mean prior has better point MSE than the independent RBF comparator in the joint scores. Test whether this relies on the correct drug-to-response-program relation rather than only a shared context correction. In each full-support scenario (primary and tissue-excluded), keep the response candidate's selected prior penalty, residual penalty, blend and amplitude fixed. Permute rows of its frozen response embedding only within strata having the same number of available source-context profiles. Seeds 3702,3703,3704. Keep the measured response tensor, its missingness mask, baselines and RBF interpolation unchanged. The embedding dimension and singular spectrum remain unchanged. Do not ensemble predictions or choose a favourable seed.

Fit all six controls, preserve predictions and their hashes, then score them with the identical saved truth and exact-reference-matched PairEvaluator. Correct-program versus shuffled-program differences are mechanistic association diagnostics, not randomized biological p-values. Independently tuned ordinary RBF and shared-context-prior baselines remain mandatory comparators. Retain all adverse metrics and explicitly separate full-response error from cross-context drug interaction risk.
