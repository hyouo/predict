# SciPlex metadata lock (before numeric-outcome analysis)

Acquired file: 139,848,567 bytes, SHA256 8ad35a20e44bf41cb030a0f063eb6a5727965b8f27d0eda4380bda0e294ccc4c. Shape 4,974 x 56,799. All 24h source/target pairs among K562 (CVCL_0004), A549 (CVCL_0023), MCF7 (CVCL_0031) will be evaluated. Only A549 has 72h records, so 72h is ineligible for cross-cell-line validation. Primary dose 10uM (stored as 10.000000000000002; compare with atol=rtol=1e-8).

At 24h/10uM, 186 drug names are shared by all 3 cell lines. Sort names by SHA256 of UTF-8 `query-split-20260907|` plus the exact published name. The first round(0.4*186)=74 are queries; the other 112 form the calibration pool. Each calibration order is a numpy default_rng permutation of the lexicographically sorted pool, for seeds 202609070..202609074. Primary budget 16, primary seed 202609070; nested 4/8/32 and remaining seeds are sensitivity. Exact saved split JSON SHA256 ddadaae32f834deda8a54193e3a70d73bde58f1129b9f9b0e01cdd54dacb08d5. Version/package hashes will be included with code.

At 24h each cell line has 16 standardized plate identifiers and 32 controls, exactly two controls per plate. Sort control well labels lexicographically within plate: first is target calibration reference, second is target scoring reference. Source effects use both controls. All models use identical references. Swapped and pooled target references are specified sensitivity checks. Do not call these different donors or assume plate labels establish independence.

There is no explicit retained replicate field. Most selected drug/cell pairs have two treatment records, nine have one, ENMD-2076 has four per cell. Primary uses every recorded condition without zero-filling or outcome-based exclusions. Different-record stress tests must report their restricted eligible set separately. Sample keys are standardized plate plus well, never well label alone.

Primary full-panel analysis retains all 56,799 genes. Secondary preprocessing analysis selects source-control-expressed genes using CPM >= 1 in at least half of source 24h control records, without accessing target treatment/query values. The same gene panel is used for all methods in each transfer; gene universe and source controls are held fixed across calibration budgets. This analysis is predeclared, not chosen after observing test errors. Keep original library totals; verify their relationship to supplied count sums, and disclose processing discrepancies rather than silently renormalizing.

All conditions are from public processed data. Prior project exploration used small SciPlex-derived gene lists, so this is independent-of-OP3 quantitative replication, not a claim that the whole study was previously unseen. No model architecture is changed for this test; quantify all strong-baseline and failure results. Official Springer Nature LaTeX package successfully acquired from publisher (901,814 bytes; SHA256 812e76dcaa9c28dc1bff1fb6065d51729b67d4ea140552a05088317414a3ecae).
