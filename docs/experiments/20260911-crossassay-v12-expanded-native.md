# v12 stage C: reference-restored exact-native-cell feasibility

The metadata-only registry audit is complete: adding the16 traceable omitted vehicles would reference all1,647 original RNA treatment records. Exact P100/RNA condition coverage becomes533; the six publisher-base-cell intersection would become88 compounds rather than30. However, many additional RNA records have native cell NPC.TAK while P100 is NPC. Publisher base-cell grouping does not establish identical preparation or differentiation conditions.

This distinct secondary analysis therefore uses ONLY the five exact-native cancer cell labels A375,A549,MCF7,PC3,YAPC, with the metadata-defined88 common compound/dose conditions and all978 measured RNA outputs. Do not use NPC/NPC.TAK in fitting, validation or evaluation. Keep the original six-cell30-condition Stage B and recorded earlier Stage A untouched. Absolute errors between these cohorts are not directly compared. This is retrospective conditional-mean feasibility, NOT independent culture validation or a query-free probe experiment.

Use the exact recovered Stage B estimator implementation, source-only inner/outer complete-cell leaveout, ridge grid .01,.1,1,10,100,1000,10000, RBF widths .1,1,10, amplitude[0,2], and source,zero,baseline-RBF,magnitude-only,missingness-only,full-P100 predictors. No new feature search or neural architecture. Source response for every query drug remains allowed; target query P1003h is allowed, target RNA6h is not supplied to fit or selection; GCP24h is not an input. Five fixed within-processing-block target P100 permutations per full/magnitude model retain selected parameters and amplitude. Shared references, unknown culture independence and prior dataset exposure are explicit.

Only missing RNA vehicles are supplemented through the approved exact-value registry; original present RNA references and all P100 references remain unchanged. No outcome-dependent gene/drug filtering or transformation is fitted between releases. Prepare inputs, freeze all predictions, then score all target cells. Report per-cell/overall/centered errors, positive/negative controls and failures. No enlarged-cohort result replaces the original negative comparison. No default-model replacement or publication claim.
