# Secondary complementary-information candidate

Declared before this turn's target scoring, after observing source-validation results for the initial candidate families. Existing test contexts were already exposed in earlier research; this is development, not confirmatory registration.

Test whether external mechanism information complements rather than replaces measured source-response information. Concatenate the existing 32-dimensional uncentred source-response embedding with the existing 32-dimensional external target/MOA embedding, divided by sqrt(2). Use the identical support-centred bilinear mean and kernel-residual update, context rank at most 12, prior/residual penalties, blend grid and analytical [0,1] scalar validation. The matched-capacity control concatenates the same source-response embedding with the covered-row-permuted target/MOA embedding. Correct and permuted annotations have identical dimensions, row coverage and singular spectra. No target treated outcomes contribute to either embedding.

Evaluate both full-source scenarios and the three previously registered 3-source masks per scenario. Do not discard earlier candidates or overwrite their predictions. Save separate selected hyperparameters, complete prediction hashes, and explicit model-family provenance. A joint model has more parameters than the response-only prior; compare primarily to its same-capacity shuffled-annotation control as well as the independently tuned RBF baseline. Report annotated and unannotated subsets. Unknown annotation rows do not encode inactivity: the joint model retains the source-response branch on those rows.
