# Secondary source-information budget and attribution test

2026-09-09. Declared after full-support validation fits but before this turn's target scoring. The study/test contexts were already used in earlier research, so these remain retrospective sensitivity analyses. Primary protocol and all negative results remain unchanged.

Hypothesis: mechanism information may have less incremental value when the same compound already has many measured source responses. Repeat the unchanged v7 estimator families with at most 3 treated source contexts per compound. Before any response representation, mean-prior fitting, kernel fitting or validation selection, discard the other compound-context response profiles. Select profiles using deterministic SHA256(seed, compound ID, context ID) order and seeds 2702, 2703, 2704; no response values or annotation status influence selection. Apply separately to the training pool and final train+validation pool. All methods receive the exact same retained response profiles; baseline controls for all allowed source contexts remain available. Retain drugs below the usual >=3-source scoring threshold as excluded, never as zero labels. No source replicate or hidden withheld profile may enter a cached embedding. Report each seed; do not average predictions to create an extra ensemble.

Run primary-source and tissue-excluded-source scenarios. Use the original hyperparameter grids, at-most-12 context / 32 drug features, matched zero/source/RBF/linear baselines and covered-row annotation shuffle. These masks change training information and are not independent biological replication or unseen-drug prediction.

For full-support selected candidates also save component-removal controls at the SAME chosen kernel penalty and amplitude (gamma=0) to isolate innovation from scalar calibration. Save mechanism prediction at the baseline model's amplitude as a diagnostic. These controls are not substitutes for the independently tuned RBF comparator. Where all mechanism features are zero, the unscaled innovation must be zero exactly; any remaining difference after global amplitude calibration must be attributed to amplitude, not external knowledge.

All completed sparse-source and attribution predictions must be saved and hash-frozen before this turn's full-support/sparse-support joint scoring. Evidence packages must record retained source masks, source and target IDs, projection hashes, source coverage, selected configurations and conditional uncertainty. No changes to the statistical claims or journal manuscript based only on these experiments.
