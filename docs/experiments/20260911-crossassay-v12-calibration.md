# v12 stage B: matched amplitude attribution (post-primary development)

The initial six-cell,30-condition P1003h-to-RNA6h feasibility has been scored. Source-scalar MSE=0.5270544, magnitude-only=0.5209457, unscaled cross-assay residual=0.5778524; all six cross-assay fits selected the upper original ridge penalty100. These observations are retained unchanged, not confirmation of absent biological P100 information.

A fairness issue is explicit: the magnitude/scalar methods can shrink the source response globally, whereas the cross-assay residual model preserves source mean amplitude. Stage B will give source mean, untreated-baseline RBF, missingness-only and full P100 residual predictors the same single amplitude calibration. For each outer held cell, obtain complete out-of-source-cell predictions from the other five cells; select ridge/width and a scalar a in[0,2] by inner context-macro MSE. Refit on the permitted sources and multiply prediction by the selected a. Neither target RNA nor scoring references enter this selection. No change to raw means, cohort, units, query information, measured gene panel or control matching. Extend cross-assay/missingness ridge grid to1000,10000 as explicit cap sensitivity; retain original grid outcomes and the zero-correction source-mean comparator. New results remain retrospective development, not a restored blind test.

For source-validation-selected calibrated cross-assay and magnitude-only models, freeze correct and five fixed query-label permutations within each original processing-block signature. Match all parameters, amplitudes and output dimensions. Do not retune permutation controls or select a favourable shuffle. Report all six cell results, not just a new minimum macro score. The magnitude model is an activity/sensitivity control, not recovery of a distinct signalling mechanism.

Freeze all stage B predictors before its scoring. Replicate the computations independently, include original negative results and common-response/centered-error decomposition. No claim of independent culture linkage, counterfactual mediation, or original query-free target prediction. No default model replacement, automatic merge or paid compute.
